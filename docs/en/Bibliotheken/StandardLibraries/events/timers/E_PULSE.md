# E_PULSE
**Important note: This function block requires only one event and no cyclic calls. It has no output ET and does not display the elapsed time.**
![image](https://github.com/user-attachments/assets/f5119723-ac8b-477f-afe1-795808faa1e2)
* * * * * * * * * *
![E_PULSE](E_PULSE.svg)
The following text was generated by <https://chat.deepseek.com/> on February 5, 2025, and has not yet been proofread (HF)

### The E_PULSE function block in IEC 61499: An overview

#### Introduction

IEC 61499 is an international standard that enables the modeling of distributed industrial control systems. A central concept in this standard is the function block (FB), which serves as a fundamental component for the control logic. The **E_PULSE** (Event-driven Pulse) function block is an example of such a function block used in IEC 61499 to generate a pulse of a specific duration. This paper describes the structure, behavior, and application of the E_PULSE function block using the provided source code. A particularly noteworthy aspect is that this function block **also functions when called non-cyclically** because it is internally based on the **E_DELAY** function block from IEC 61499.

#### Structure of the E_PULSE Function Block

The E_PULSE function block is a **Composite Function Block (CFB)** specified according to IEC 61499. A CFB consists of an interface and an internal network of function blocks that work together to achieve the desired behavior.

##### Interface

The interface of the E_PULSE block consists of:

- **Event Inputs**:
- **REQ (Request)**: This event triggers the execution of the block. It is linked to the input variable **PT** (Pulse Time).
- **R (Reset)**: This event resets the timer.
- **Event Output**:
- **CNF (Confirmation)**: This event is triggered when the execution of the block is complete. It is linked to the output variable **Q**.
- **Input Variable**:
- **PT (Pulse Time)**: This is a time variable (TIME) that defines the duration of the pulse.
- **Output Variable**:
- **Q (Output)**: This is a Boolean variable that indicates the state of the pulse. It is **TRUE** as long as the pulse is active.

#### Internal Network (FBNetwork)

The E_PULSE block uses two internal function blocks to implement the desired behavior:

1. **E_DELAY**: This block implements the delay time **PT**. When the **START** event occurs, the timer starts running. When the delay time has elapsed, the **EO** event is triggered.

2. **E_SR**: This block is a bistable element that stores the pulse state. When the **S** event occurs, the output **Q** is set to **TRUE**. When the **R** event occurs, the output **Q** is set to **FALSE**.

#### Behavior of the E_PULSE Block

The E_PULSE block behaves like a pulse generator that produces a pulse with a specific duration **PT**. The behavior of the function block can be described as follows:

1. **Pulse Start**: When the **REQ** event occurs, the timer is started, and the output **Q** is set to **TRUE**.

2. **Pulse Duration**: The output **Q** remains **TRUE** until the delay time **PT** has elapsed.

3. **Pulse End**: Once the delay time **PT** has elapsed, the output **Q** is set to **FALSE**, and the **CNF** event is triggered to signal the end of the pulse.

4. **Reset**: When the **R** event occurs, the timer is reset, and the output **Q** is set to **FALSE**.

#### Independence from Cyclic Calling

A key advantage of the E_PULSE function block is that it **does not need to be called cyclically** to function correctly. This is because the function block is internally based on the **E_DELAY** function block, which is specified in IEC 61499. The **E_DELAY** function block is an event-driven timer that operates independently of the cyclic call of the parent function block. This means that the E_PULSE function block can also be used in systems that do not operate cyclically, such as event-driven or distributed control systems.

#### Application Examples

The E_PULSE function block can be used in various industrial control applications, especially in scenarios where a pulse of a specific duration is required. Some examples are:

- **Machine Control**: The function block can be used to generate a short pulse that activates a machine for a specific period of time, for example, to start or stop a process.
- **Lighting Control**: This function block can be used in lighting systems to switch on the light for a specific period of time, e.g., for emergency lighting.
- **Valve Control**: This function block can be used to open a valve for a specific period of time, e.g., to release a specific quantity of liquid or gas.

##

## 🛠️ Related Exercises
* [Exercise_020h](../../../../Uebungen/test_B/Uebungen_doc/Uebung_020h.md)]
* [Exercise_020i](../../../../Uebungen/test_B/Uebungen_doc/Uebung_020i.md)]

## Conclusion

The E_PULSE function block is a versatile function block in IEC 61499 that implements a pulse with a specific duration. Due to its internal use of the **E_DELAY** function block, it can function correctly **even when not called cyclically**. This makes it particularly suitable for event-driven and distributed control systems where cyclic calls are not always guaranteed.

The provided source code demonstrates how the E_PULSE function block can be implemented in practice and offers a solid foundation for developing similar function blocks in industrial control systems. Its independence from cyclic calls is a crucial advantage that makes the function block suitable for a wide range of applications.