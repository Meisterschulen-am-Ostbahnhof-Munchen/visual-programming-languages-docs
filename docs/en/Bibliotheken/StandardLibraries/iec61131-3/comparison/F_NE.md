# F_NE

![F_NE](https://user-images.githubusercontent.com/116869307/214144558-e676465a-a116-46b2-87e7-fd3b912c702a.png)
<https://content.helpme-codesys.com/de/CODESYS%20Development%20System/_cds_operator_ne.html>
<https://content.helpme-codesys.com/en/CODESYS%20Development%20System/_cds_operator_ne.html>
* * * * * * * * * *
![F_NE](F_NE.svg)

The following text was generated by <https://chat.deepseek.com/> on February 5, 2025, and has not yet been proofread (HF)

**The NE Operator in IEC 61499: An Introduction**

IEC 61499 is an international standard that deals with the modeling of function blocks for industrial automation systems. It provides a standardized framework for the development of distributed control systems, in which logical and arithmetic operators play a central role. One of these operators is the **NE operator** (Not Equal), which will be examined in more detail in this article.

### What is the NE operator?

The NE operator is a comparison operator used to determine whether two values are unequal. In IEC 61499, this operator is frequently used in function blocks to evaluate conditions and make decisions based on comparisons. The NE operator compares two input values and returns a Boolean value (`TRUE` or `FALSE`) depending on whether the values are unequal.

### Syntax and Usage

In IEC 61499, the NE operator is used in function block expressions. The syntax of the NE operator is as follows:

NE(Eingang1, Eingang2)
Here, `Eingang1` and `Eingang2` are the two values to be compared. The operator returns `TRUE` if `Eingang1` is not equal to `Eingang2`; otherwise, the return value is `FALSE`.

### Application Examples

1. **Monitoring Sensor Values:**

In industrial processes, it is often necessary to monitor sensor values. The NE operator can be used to check whether two sensors are providing different values:

IF NE(Sensor1, Sensor2) THEN
// Alarm auslösen oder Korrekturmaßnahmen einleiten
END_IF

2. **Control of Production Processes:**

In a production process, the NE operator can be used to determine whether a specific process step is not yet complete. If the current step is not the target step, the process can continue:

IF NE(AktuellerSchritt, ZielSchritt) THEN
// Prozess fortsetzen
END_IF

3. **Temperature Control:**

In a heating system, the NE operator can be used to check if the current temperature is not equal to the set temperature. If this is the case, the heating can remain activated:

IF NE(AktuelleTemperatur, SollTemperatur) THEN
// Heizung weiter aktivieren
END_IF

4. **Error Detection:**

The NE operator can also be used for error detection. For example, if a setpoint and an actual value are compared and they do not match, an error signal can be generated:

IF NE(Sollwert, Istwert) THEN
// Fehlersignal generieren
END_IF
### Advantages of the NE Operator

- **Simple Logic:** The NE operator allows complex conditions to be formulated in simple, easily understandable expressions.
- **Flexibility:** The operator can be applied to various data types, including integers, floating-point numbers, and even Boolean values.
- **Efficiency:** By being directly integrated into function blocks, the NE operator reduces the need for additional programming and makes the code more efficient.
- **Robustness:** The NE operator helps develop robust control algorithms based on precise comparisons.

### Conclusion

The NE operator is an important tool in IEC 61499 that simplifies the implementation of comparisons in industrial control systems. Its ability to determine whether two values are unequal makes it an indispensable component in process monitoring, control, and decision-making. Whether in production control, temperature regulation, or fault detection – the NE operator is a powerful and versatile element in the world of industrial automation. Its simplicity and efficiency contribute to the development of reliable and effective control systems.
